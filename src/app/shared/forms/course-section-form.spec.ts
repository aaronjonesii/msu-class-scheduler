import { CourseSectionForm } from './course-section-form';

describe('CourseSectionForm', () => {
  it('should create an instance', () => {
    expect(new CourseSectionForm()).toBeTruthy();
  });
});
