import { ColorToClassPipe } from './color-to-class.pipe';

describe('ColorToClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
